import { ReactNode } from 'react';
export interface CarouselItemProps {
    animation: 'fade' | 'slide';
    next?: Function;
    prev?: Function;
    state: {
        active: number;
        prevActive: number;
        next: boolean;
    };
    swipe?: boolean;
    index: number;
    maxIndex: number;
    duration: number;
    child: ReactNode;
    setHeight: Function;
}
export declare const CarouselItem: ({ animation, next, prev, swipe, state, index, maxIndex, duration, child, setHeight }: CarouselItemProps) => JSX.Element;
